# Per-ticket run logs — ephemeral, may contain ticket-specific content; never committed.
logs/

# Persistent execution history (store.py) — local machine state, never committed.
monitor.db
monitor.db-wal
monitor.db-shm

# Real Jira token — never committed. Copy .env.example to .env and fill it in.
.env
